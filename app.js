const BACKEND_DATA={"users":[{"user_id":"B001","name":"Vincent William","email":"vincentw@smamarsudirinibekasi.sch.id","password_hash":"e547bd13228250dfb4c7df1d1ebb78cfd9f2ada56ebb0c425d35829dd3ac4ae8","role":"buyer"},{"user_id":"B002","name":"Bryan Tristan","email":"bryantt@smamarsudirinibekasi.sch.id","password_hash":"e547bd13228250dfb4c7df1d1ebb78cfd9f2ada56ebb0c425d35829dd3ac4ae8","role":"buyer","wishlist":["P002"]},{"user_id":"B003","name":"Hernawan Charis","email":"nawancharis@smamarsudirinibekasi.sch.id","password_hash":"e547bd13228250dfb4c7df1d1ebb78cfd9f2ada56ebb0c425d35829dd3ac4ae8","role":"buyer"},{"user_id":"B004","name":"Ecclesia Miracle","email":"emiracle@smamarsudirinibekasi.sch.id","password_hash":"e547bd13228250dfb4c7df1d1ebb78cfd9f2ada56ebb0c425d35829dd3ac4ae8","role":"buyer"},{"user_id":"S001","name":"Bu Linda","email":"bulinda@marsteen.sch.id","password_hash":"2a76110d06bcc4fd437337b984131cfa82db9f792e3e2340acef9f3066b264e0","role":"seller","tenant":"Bu Linda"},{"user_id":"S002","name":"Opung Tobing","email":"opungtobing@marsteen.sch.id","password_hash":"2a76110d06bcc4fd437337b984131cfa82db9f792e3e2340acef9f3066b264e0","role":"seller","tenant":"Opung Tobing"},{"user_id":"S003","name":"Mama Jones","email":"mamajones@marsteen.sch.id","password_hash":"2a76110d06bcc4fd437337b984131cfa82db9f792e3e2340acef9f3066b264e0","role":"seller","tenant":"Mama Jones"},{"user_id":"S004","name":"Pakde Geprek","email":"pakdegeprek@marsteen.sch.id","password_hash":"2a76110d06bcc4fd437337b984131cfa82db9f792e3e2340acef9f3066b264e0","role":"seller","tenant":"Pakde Geprek"},{"user_id":"S005","name":"Warung Nyai","email":"seller@marsteen.sch.id","password_hash":"2a76110d06bcc4fd437337b984131cfa82db9f792e3e2340acef9f3066b264e0","role":"seller","tenant":"Warung Nyai"}],"products":[{"id":"P001","tenant":"Bu Linda","name":"Chicken Katsu Rice","category":"Rice Bowl","price":18000,"stock":16,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":22000,"description":"Nasi hangat dengan chicken katsu dan saus."},{"id":"P002","tenant":"Opung Tobing","name":"Ayam Black Pepper","category":"Rice Bowl","price":20000,"stock":5,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":24000,"description":"Ayam black pepper dengan kentang goreng."},{"id":"P003","tenant":"Pakde Geprek","name":"Ayam Geprek Level 5","category":"Rice Bowl","price":17000,"stock":10,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":21000,"description":"Ayam pedas gurih untuk pecinta rasa kuat."},{"id":"P004","tenant":"Opung Tobing","name":"Kebab","category":"Snack","price":10000,"stock":18,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":13000,"description":"Kebab dengan isian daging dan sayuran."},{"id":"P005","tenant":"Warung Nyai","name":"Mac n Cheese","category":"Snack","price":15000,"stock":25,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":12000,"description":"Makaroni keju enak."},{"id":"P006","tenant":"Mama Jones","name":"Nasi Nugget","category":"Rice Bowl","price":12000,"stock":20,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":15000,"description":"Nasi hangat dengan nugget."},{"id":"P007","tenant":"Warung Nyai","name":"Spageti Carbonara","category":"Rice Bowl","price":8000,"stock":30,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":10000,"description":"Spageti carbonara creamy ala Nyai."},{"id":"P008","tenant":"Bu Linda","name":"Nasi Hainan","category":"Rice Bowl","price":15000,"stock":19,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":5000,"description":"Nasi Hainan Hangat untuk semua lauk"},{"id":"P009","tenant":"Opung Tobing","name":"Ayam Teriyaki","category":"Rice Bowl","price":15000,"stock":20,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":5000,"description":"Ayam Teriyaki yang enak dan nikmat"},{"id":"P010","tenant":"Opung Tobing","name":"Ayam Kuluyuk","category":"Rice Bowl","price":15000,"stock":10,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":5000,"description":"Ayam Kuluyuk yang nikmat"},{"id":"P011","tenant":"Pakde Geprek","name":"Ayam Geprek Original","category":"Rice Bowl","price":15000,"stock":13,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}],"king_price":18000,"description":"Ayam Original tidak pedas"}],"orders":[{"order_id":"ORD-20260907165900-90","buyer_id":"B001","buyer_name":"Vincent William","items":[{"product_id":"P001","name":"Chicken Katsu Rice","tenant":"Bu Linda","quantity":18,"unit_price":25000,"king_size":true,"addons":[{"name":"Telur","price":3000}]}],"total":450000,"queue_number":1,"status":"Siap Diambil","payment_status":"PAID","created_at":"07/09/2026 16:59","pickup_reminder":"Pengingat aktif — cek kembali saat pesanan sudah siap diambil."},{"order_id":"ORD-20260907165932-39","buyer_id":"B001","buyer_name":"Vincent William","items":[{"product_id":"P001","name":"Chicken Katsu Rice","tenant":"Bu Linda","quantity":18,"unit_price":18000,"king_size":false,"addons":[]}],"total":324000,"queue_number":2,"status":"Siap Diambil","payment_status":"PAID","created_at":"07/09/2026 16:59","pickup_reminder":"Pengingat aktif — cek kembali saat pesanan sudah siap diambil."},{"order_id":"ORD-20260907170214-33","buyer_id":"B002","buyer_name":"Bryan Tristan","items":[{"product_id":"P002","name":"Ayam Black Pepper","tenant":"Opung Tobing","quantity":15,"unit_price":20000,"king_size":false,"addons":[]},{"product_id":"P002","name":"Ayam Black Pepper","tenant":"Opung Tobing","quantity":15,"unit_price":20000,"king_size":false,"addons":[]},{"product_id":"P002","name":"Ayam Black Pepper","tenant":"Opung Tobing","quantity":15,"unit_price":20000,"king_size":false,"addons":[]}],"total":900000,"queue_number":3,"status":"Sedang Diproses","payment_status":"PAID","created_at":"07/09/2026 17:02","pickup_reminder":"Pengingat aktif — cek kembali saat pesanan sudah siap diambil."},{"order_id":"ORD-20260908101846-78","buyer_id":"B001","buyer_name":"Vincent William","items":[{"product_id":"P003","name":"Ayam Geprek Level 5","tenant":"Pakde Geprek","quantity":1,"unit_price":17000,"king_size":false,"addons":[]}],"total":17000,"queue_number":4,"status":"Siap Diambil","payment_status":"PAID","created_at":"08/09/2026 10:18","pickup_reminder":"Belum ada pengingat"},{"order_id":"ORD-20260908143813-91","buyer_id":"B001","buyer_name":"Vincent William","items":[{"product_id":"P001","name":"Chicken Katsu Rice","tenant":"Bu Linda","quantity":2,"unit_price":22000,"king_size":true,"addons":[]}],"total":44000,"queue_number":5,"status":"Siap Diambil","payment_status":"PAID","created_at":"08/09/2026 14:38","pickup_reminder":"Pengingat aktif — cek kembali saat pesanan sudah siap diambil."},{"order_id":"ORD-20260908152406-99","buyer_id":"B001","buyer_name":"Vincent William","items":[{"product_id":"P008","name":"Nasi Hainan","tenant":"Bu Linda","quantity":1,"unit_price":17000,"king_size":false,"addons":[{"name":"Saus Keju","price":2000}],"notes":"Yang panas aja bu"}],"total":17000,"queue_number":7,"status":"Siap Diambil","payment_status":"PAID","created_at":"08/09/2026 15:24","pickup_reminder":"Pengingat aktif — cek kembali saat pesanan sudah siap diambil."}],"queue":7,"addons":[{"name":"Saus Keju","price":2000},{"name":"Saus Pedas","price":1000},{"name":"Telur","price":3000},{"name":"Extra Sambal","price":0}]};
const KEY='marsteen_web_v4';
const EMOJI=['🍗','🥘','🌶️','🌯','🧀','🍱','🍝','🍚','🍗','🍗','🌶️'];
const steps=['Menunggu Pembayaran','Menunggu Konfirmasi Penjual','Sedang Diproses','Siap Diambil','Selesai'];
let saved=JSON.parse(localStorage.getItem(KEY)||'null');
let db=saved?.db?normalize(saved.db):normalize(structuredClone(BACKEND_DATA));
let s=saved?.session||{role:null,user:null,page:'home',query:'',category:'All',tenant:null};
let cart=saved?.cart||[];let wish=saved?.wish||[];let paymentProofs=saved?.paymentProofs||{};let qrUploads=saved?.qrUploads||{};
function normalize(d){d.orders=d.orders||[];d.products=d.products||[];d.users=d.users||[];d.addons=d.addons||[];d.archived_orders=d.archived_orders||[];d.archived_sales=d.archived_sales||{};return d}
function persist(){localStorage.setItem(KEY,JSON.stringify({db,session:s,cart,wish,paymentProofs,qrUploads}))}
function esc(x=''){return String(x).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function rupiah(n){return 'Rp'+Number(n||0).toLocaleString('id-ID')}
function user(){return s.user}
function prod(id){return db.products.find(p=>p.id===id)}
function userOrders(){return db.orders.filter(o=>o.buyer_id===user()?.user_id)}
function sellerOrders(){return db.orders.filter(o=>o.items?.some(i=>i.tenant===user()?.tenant))}
function cartQty(){return cart.reduce((a,x)=>a+x.quantity,0)}
function cartTotal(){return cart.reduce((a,x)=>a+x.quantity*x.unit_price,0)}
function toast(t){const el=document.getElementById('toast');el.textContent=t;el.className='toast show';setTimeout(()=>el.className='toast',2200)}
function logout(){s={role:null,user:null,page:'home',query:'',category:'All',tenant:null};cart=[];persist();render()}
async function sha256(x){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(x));return [...new Uint8Array(b)].map(v=>v.toString(16).padStart(2,'0')).join('')}
function entrance(){document.body.innerHTML=`<div class="login"><section class="login-left"><div><div class="brand"><div class="mark">M</div>MARSTEEN</div><div style="margin-top:12vh"><div class="eyebrow" style="color:#efb28f">SCHOOL FOOD ORDERING</div><h1>Lunch, without the <em>canteen chaos.</em></h1><p>Pesan lebih dulu, sesuaikan menu, bayar per tenant, lalu ikuti antrean sampai pesanan siap diambil.</p></div></div><div class="principles"><span>ORDER AHEAD</span><span>PAY PER TENANT</span><span>LIVE ORDER TRACKING</span></div></section><section class="login-right"><div class="auth-card"><div class="eyebrow">WELCOME TO MARSTEEN</div><h2>Choose your workspace</h2><p class="sub">Masuk menggunakan akun yang sudah terdaftar di backend MARSTEEN.</p><div class="roles"><button id="rb" class="on" onclick="loginCard('buyer')">Pembeli</button><button id="rs" onclick="loginCard('seller')">Penjual</button></div><div id="authfields"></div></div></section></div>`;loginCard('buyer')}
function loginCard(role){s.role=role;document.querySelectorAll('.roles button').forEach((b,i)=>b.classList.toggle('on',(role==='buyer'?i===0:i===1)));document.getElementById('authfields').innerHTML=`<div class="field"><label>Email</label><input id="email" class="input" autocomplete="username" placeholder="${role==='buyer'?'yourname@smamarsudirinibekasi.sch.id':'yourname@marsteen.sch.id'}"></div><div class="field"><label>Password</label><input id="password" type="password" class="input" autocomplete="current-password" placeholder="Enter your password"></div><button class="btn primary block" onclick="doLogin('${role}')">Masuk sebagai ${role==='buyer'?'Pembeli':'Penjual'} →</button><p class="sub" style="font-size:11px;margin-top:13px">${role==='buyer'?'Gunakan email sekolah yang terdaftar.':'Gunakan akun penjual yang telah diverifikasi admin.'}</p>`}
async function doLogin(role){const e=document.getElementById('email').value.trim(),p=document.getElementById('password').value;if(!e||!p)return toast('Email dan password wajib diisi');if(role==='buyer'&&!e.toLowerCase().endsWith('@smamarsudirinibekasi.sch.id'))return toast('Email pembeli harus memakai domain sekolah');if(role==='seller'&&!e.toLowerCase().endsWith('@marsteen.sch.id'))return toast('Email penjual harus memakai domain MARSTEEN');const h=await sha256(p);const u=db.users.find(x=>x.role===role&&x.email.toLowerCase()===e.toLowerCase()&&x.password_hash===h);if(!u)return toast('Email atau password tidak sesuai');s={...s,role,user:u,page:role==='buyer'?'home':'overview',query:'',category:'All',tenant:null};if(role==='buyer')wish=u.wishlist||[];persist();render();toast('Selamat datang di MARSTEEN')}
function nav(side){let buyer=[['home','⌂','Beranda'],['tenant','▦','Tenant'],['cart','🛒','Keranjang'],['wishlist','♡','Wishlist'],['orders','▣','Pesanan']];let seller=[['overview','◈','Dashboard'],['products','▦','Produk'],['addons','＋','Add-ons'],['orders','▣','Pesanan'],['history','◴','Riwayat'],['reports','⌁','Laporan']];let items=s.role==='buyer'?buyer:seller;return `<aside class="sidebar"><div class="brand"><div class="mark">M</div>MARSTEEN</div><div class="tiny" style="color:#efb28f;margin:5px 9px 10px">${s.role==='buyer'?'BUYER SPACE':'SELLER SPACE'}</div><div class="side-nav">${items.map(([id,ic,t])=>`<button class="side ${s.page===id?'active':''}" onclick="s.page='${id}';s.tenant=null;persist();render()"><span>${ic}</span> ${t}${id==='cart'&&cartQty()?` · ${cartQty()}`:''}</button>`).join('')}</div><div class="side-footer"><div style="font-weight:800">${esc(user()?.name||'')}</div><div style="font-size:11px;margin-top:3px">${esc(user()?.tenant||user()?.email||'')}</div><button class="side" style="padding-left:0;margin-top:10px" onclick="logout()">↪ Keluar</button></div></aside>`}
function shell(content,title,sub=''){return `<div class="page-shell">${nav()}<main class="main"><div class="topbar"><div><div class="tiny">MARSTEEN / ${s.role==='buyer'?'PEMBELI':'PENJUAL'}</div><h1>${title}</h1>${sub?`<div class="sub">${sub}</div>`:''}</div><div class="profile-chip"><div class="avatar">${esc((user()?.name||'M')[0])}</div><span>${esc(user()?.name||'')}</span></div></div>${content}</main></div>`}
function buyerHome(){let ps=db.products.filter(p=>(s.category==='All'||p.category===s.category)&&(!s.tenant||p.tenant===s.tenant)&&(!s.query||`${p.name} ${p.tenant} ${p.category}`.toLowerCase().includes(s.query.toLowerCase())));let cats=['All',...new Set(db.products.map(p=>p.category))];return shell(`<section class="hero"><div><div class="eyebrow" style="color:#efb28f">TODAY AT THE CANTEEN</div><h2>What are you<br>craving today?</h2><p>Explore menu, customize your order, and secure your queue before lunch gets busy.</p><button class="btn coral" onclick="document.getElementById('search').focus()">Browse the menu ↓</button></div><div class="hero-stat"><span class="sub" style="color:#d8e0e2">Available menu</span><strong>${db.products.filter(p=>p.stock>0).length}</strong><span class="sub" style="color:#d8e0e2">items ready to order</span></div></section><div class="toolbar"><input id="search" class="input search" placeholder="Cari makanan, tenant, kategori..." value="${esc(s.query)}" oninput="s.query=this.value;render()"><div class="chips">${cats.map(c=>`<button class="chip ${s.category===c?'active':''}" onclick="s.category='${esc(c)}';render()">${esc(c)}</button>`).join('')}</div></div><div class="section-title"><div><div class="tiny">MENU</div><h2>Recommended today</h2></div><span class="sub">${ps.length} item</span></div><div class="food-grid">${ps.length?ps.map(card).join(''):`<div class="empty" style="grid-column:1/-1">Tidak ada menu yang cocok.</div>`}</div>`, 'Beranda','Pesan makanan kantin dengan alur yang sama seperti sistem backend MARSTEEN.')}
function card(p){let fav=wish.includes(p.id),idx=Math.max(0,parseInt(p.id.slice(1))-1);return `<article class="food"><div class="food-art"><span>${esc(p.category)}</span>${EMOJI[idx%EMOJI.length]}<button class="heart" onclick="toggleWish('${p.id}')">${fav?'♥':'♡'}</button></div><h3>${esc(p.name)}</h3><div class="sub">${esc(p.tenant)}</div><div class="food-foot"><div><div class="price">${rupiah(p.price)}</div><div class="stock ${p.stock?'':'out'}">${p.stock?'Stok '+p.stock:'HABIS'}</div></div><button class="btn ${p.stock?'primary':''}" ${p.stock?'':'disabled'} onclick="productModal('${p.id}')">${p.stock?'Tambah':'Habis'}</button></div></article>`}
function toggleWish(id){if(!user())return;wish=wish.includes(id)?wish.filter(x=>x!==id):[...wish,id];db.users.find(u=>u.user_id===user().user_id).wishlist=[...wish];s.user=db.users.find(u=>u.user_id===user().user_id);persist();render();toast(wish.includes(id)?'Ditambahkan ke wishlist':'Dihapus dari wishlist')}
function productModal(id){let p=prod(id);let adds=p.addons||db.addons;document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop" id="modal"><div class="modal"><div class="modal-head"><div><div class="eyebrow">${esc(p.tenant)}</div><h2 class="serif" style="font-size:32px;margin:5px 0">${esc(p.name)}</h2><div class="sub">${esc(p.description||'')}</div></div><button class="close" onclick="this.closest('.modal-backdrop').remove()">×</button></div><div class="panel" style="margin-top:18px"><div class="field"><label>Ukuran</label>${p.king_price&&p.king_price>p.price?`<label class="check-row"><input type="radio" name="size" value="0" checked onchange="modalPrice('${id}')"> Regular · ${rupiah(p.price)}</label><label class="check-row"><input type="radio" name="size" value="1" onchange="modalPrice('${id}')"> King Size · ${rupiah(p.king_price)}</label>`:`<div class="sub">Ukuran regular · ${rupiah(p.price)}</div>`}</div><div class="field"><label>Add-ons / Saus</label>${adds.map((a,i)=>`<label class="check-row"><input class="addon" data-i="${i}" type="checkbox" onchange="modalPrice('${id}')"> ${esc(a.name)} <span class="sub">+${rupiah(a.price)}</span></label>`).join('')}</div><div class="field"><label>Catatan Khusus</label><input id="note" class="input" placeholder="Pedas sedang, tanpa bawang, dsb."></div><div class="field"><label>Jumlah</label><input id="qty" class="input" type="number" min="1" max="${p.stock}" value="1" onchange="modalPrice('${id}')"></div><div class="total-row"><span>Total</span><span id="mt">${rupiah(p.price)}</span></div><button class="btn primary block" style="margin-top:15px" onclick="addCart('${id}')">Tambah ke Keranjang →</button></div></div></div>`)}
function modalPrice(id){let p=prod(id),king=document.querySelector('input[name=size]:checked')?.value==='1',adds=[...document.querySelectorAll('.addon:checked')].map(x=>(p.addons||db.addons)[+x.dataset.i].price),q=Math.max(1,parseInt(document.getElementById('qty')?.value)||1),u=(king&&p.king_price?p.king_price:p.price)+adds.reduce((a,b)=>a+b,0);if(document.getElementById('mt'))document.getElementById('mt').textContent=rupiah(u*q)}
function addCart(id){let p=prod(id),king=document.querySelector('input[name=size]:checked')?.value==='1',adds=[...document.querySelectorAll('.addon:checked')].map(x=>(p.addons||db.addons)[+x.dataset.i]),q=Math.max(1,Math.min(p.stock,parseInt(document.getElementById('qty').value)||1)),u=(king&&p.king_price?p.king_price:p.price)+adds.reduce((a,b)=>a+b.price,0),item={key:JSON.stringify([id,king,adds.map(a=>a.name)]),product_id:id,name:p.name,tenant:p.tenant,quantity:q,unit_price:u,king_size:king,addons:adds,notes:document.getElementById('note').value.trim()};let old=cart.find(x=>x.key===item.key);if(old)old.quantity=Math.min(p.stock,old.quantity+q);else cart.push(item);persist();document.getElementById('modal').remove();render();toast('Produk ditambahkan ke keranjang')}
function tenants(){let ts=[...new Set(db.products.map(p=>p.tenant))];if(s.tenant){let ps=db.products.filter(p=>p.tenant===s.tenant);return shell(`<button class="btn" onclick="s.tenant=null;render()">← Semua tenant</button><div class="section-title"><div><div class="tiny">TENANT</div><h2>${esc(s.tenant)}</h2></div><span class="sub">${ps.length} menu</span></div><div class="food-grid">${ps.map(card).join('')}</div>`,'Tenant','Lihat menu per penjual.')}return shell(`<div class="tenant-list">${ts.map(t=>{let n=db.products.filter(p=>p.tenant===t).length;return `<div class="tenant-card"><div><strong>${esc(t)}</strong><div class="sub">${n} menu tersedia</div></div><button class="btn primary" onclick="s.tenant='${esc(t)}';render()">Lihat Menu →</button></div>`}).join('')}</div>`,'Tenant','Temukan semua tenant kantin dan menu mereka.')}
function cartPage(){if(!cart.length)return shell(`<div class="empty"><div style="font-size:45px">🛒</div><h2 class="serif">Keranjang masih kosong</h2><p>Pilih makanan dari menu untuk mulai membuat pesanan.</p><button class="btn primary" onclick="s.page='home';render()">Kembali ke menu</button></div>`,'Keranjang','Pesanan yang kamu siapkan sebelum checkout.');let by={};cart.forEach((x,i)=>(by[x.tenant]??=[]).push({...x,_i:i}));return shell(`<div class="split"><div>${Object.entries(by).map(([t,items])=>`<div class="panel" style="margin-bottom:13px"><div class="section-title" style="margin-top:0"><div><div class="tiny">TENANT</div><h2>${esc(t)}</h2></div><button class="btn primary" onclick="checkout('${esc(t)}')">Bayar tenant →</button></div>${items.map(x=>`<div class="cart-row"><div style="font-size:30px">${EMOJI[(parseInt(x.product_id.slice(1))-1)%EMOJI.length]}</div><div class="grow"><strong>${esc(x.name)} × ${x.quantity}</strong><div class="sub">${x.king_size?'King Size · ':''}${x.addons?.map(a=>esc(a.name)).join(', ')||'Tanpa add-on'}</div>${x.notes?`<div class="notes">📝 ${esc(x.notes)}</div>`:''}</div><strong>${rupiah(x.quantity*x.unit_price)}</strong><button class="btn danger" onclick="removeCart(${x._i})">Hapus</button></div>`).join('')}<div class="total-row"><span>Total ${esc(t)}</span><span>${rupiah(items.reduce((a,x)=>a+x.quantity*x.unit_price,0))}</span></div></div>`).join('')}</div><div class="panel" style="height:max-content;position:sticky;top:20px"><div class="tiny">ORDER SUMMARY</div><h2 class="serif" style="font-size:28px">Ready to checkout?</h2><p class="sub">Pesanan akan dipisahkan per tenant dan setiap tenant memiliki pembayaran sendiri.</p><div class="total-row"><span>${cartQty()} item</span><span>${rupiah(cartTotal())}</span></div><button class="btn coral block" style="margin-top:15px" onclick="checkout()">Checkout semua →</button></div></div>`,'Keranjang','Pisahkan pembayaran berdasarkan tenant seperti alur backend.')}
function removeCart(i){cart.splice(i,1);persist();render()}
function checkout(tenant){let items=tenant?cart.filter(x=>x.tenant===tenant):[...cart];if(!items.length)return;for(const x of items){let p=prod(x.product_id);if(!p||p.stock<x.quantity)return toast(`Stok ${x.name} tidak mencukupi`)}let missing=[...new Set(items.map(x=>x.tenant).filter(t=>!qrUploads[t]))];if(missing.length)return toast('QR belum tersedia: '+missing.join(', '));if(!confirm(`Konfirmasi pesanan ${rupiah(items.reduce((a,x)=>a+x.quantity*x.unit_price,0))}?`))return;let groups={};items.forEach(x=>(groups[x.tenant]??=[]).push(x));let created=[];for(const [t,it] of Object.entries(groups)){db.queue=(db.queue||0)+1;let total=it.reduce((a,x)=>a+x.quantity*x.unit_price,0),deadline=new Date(Date.now()+15*60000).toISOString();let o={order_id:`ORD-WEB-${Date.now()}-${Math.floor(Math.random()*90+10)}`,buyer_id:user().user_id,buyer_name:user().name,items:it.map(x=>({...x})),total,queue_number:db.queue,status:'Menunggu Pembayaran',payment_status:'UNPAID',created_at:new Date().toLocaleString('id-ID'),pickup_reminder:'Belum ada pengingat',payment_deadline:deadline,tenant_payments:[{tenant:t,amount:total,qr_path:qrUploads[t],proof_path:'',status:'UNPAID',submitted_at:''}]};db.orders.push(o);created.push(o)}let keys=new Set(items.map(x=>x.key));cart=cart.filter(x=>!keys.has(x.key));persist();s.page='orders';render();toast('Pesanan dibuat. Silakan bayar dalam 15 menit');paymentPage(created[0].order_id)}
function paymentPage(orderId){let o=db.orders.find(x=>x.order_id===orderId);if(!o)return;document.body.innerHTML=shell(paymentContent(o),'Pembayaran','Upload bukti pembayaran maksimal 15 menit.').replace('<main class="main">','<main class="main">');startTimer(o)}
function paymentContent(o){return `<div class="panel"><div class="order-head"><div><div class="tiny">PAYMENT CENTER</div><h2 class="serif" style="font-size:29px;margin:4px 0">${esc(o.order_id)}</h2><div class="sub">Antrean #${o.queue_number} · ${esc(o.created_at)}</div></div><div class="countdown" id="timer">--:--</div></div><div class="payment-grid" style="margin-top:18px">${(o.tenant_payments||[]).map(p=>`<div class="pay-card"><div class="tiny">PAY TO TENANT</div><h3 style="margin:5px 0;font-size:20px">${esc(p.tenant)}</h3><strong>${rupiah(p.amount)}</strong><div class="qr" style="margin:13px 0">${qrUploads[p.tenant]?`<img src="${qrUploads[p.tenant]}">`:`<div class="fakeqr"></div>`}</div><div class="sub">Status pembayaran</div><div style="margin:6px 0 12px"><span class="status ${p.status==='PAID'?'':'warn'}">${esc(p.status)}</span></div><label class="btn primary block">Upload bukti PNG<input type="file" accept=".png,image/png" hidden onchange="submitProof('${o.order_id}','${esc(p.tenant)}',this)"></label>${paymentProofs[o.order_id+'|'+p.tenant]?`<div class="sub" style="margin-top:8px">✓ Bukti tersimpan di browser</div>`:''}</div>`).join('')}</div><p class="sub" style="margin-top:16px">Hanya mendukung format PNG. Setelah semua tenant mengirim bukti, status order menjadi <b>Menunggu Konfirmasi Penjual</b>.</p><button class="btn" onclick="s.page='orders';persist();render()">Selesai / Lihat Pesanan</button></div>`}
function startTimer(o){let el=document.getElementById('timer');if(!el)return;let deadline=new Date(o.payment_deadline).getTime();let tick=()=>{let left=Math.max(0,deadline-Date.now());if(!left){o.status='Dibatalkan';o.payment_status='EXPIRED';(o.tenant_payments||[]).forEach(p=>{if(p.status==='UNPAID'||p.status==='SUBMITTED')p.status='EXPIRED'});persist();el.textContent='00:00';return}let sec=Math.floor(left/1000),m=Math.floor(sec/60),ss=sec%60;el.textContent=`${String(m).padStart(2,'0')}:${String(ss).padStart(2,'0')}`;setTimeout(tick,1000)};tick()}
function submitProof(oid,tenant,input){let f=input.files[0];if(!f)return;if(f.type!=='image/png'&&!f.name.toLowerCase().endsWith('.png'))return toast('HANYA MENDUKUNG FORMAT PNG');let reader=new FileReader();reader.onload=()=>{paymentProofs[oid+'|'+tenant]=reader.result;let o=db.orders.find(x=>x.order_id===oid),p=o.tenant_payments.find(x=>x.tenant===tenant);p.status='SUBMITTED';p.proof_path='browser://'+f.name;p.submitted_at=new Date().toISOString();o.payment_status=o.tenant_payments.every(x=>x.status==='SUBMITTED'||x.status==='PAID')?'SUBMITTED':'PARTIAL';o.status='Menunggu Konfirmasi Penjual';persist();paymentPage(oid);toast('Bukti pembayaran berhasil diunggah')};reader.readAsDataURL(f)}
function wishlistPage(){let ps=wish.map(prod).filter(Boolean);return shell(`<div class="section-title"><div><div class="tiny">SAVED FOR LATER</div><h2>Menu Favorit</h2></div><button class="btn primary" onclick="s.page='home';render()">Explore menu →</button></div><div class="food-grid">${ps.length?ps.map(card).join(''):`<div class="empty" style="grid-column:1/-1">Belum ada menu favorit.</div>`}</div>`,'Wishlist','Simpan menu yang ingin kamu pesan lagi.')}
function ordersPage(){let os=userOrders().slice().reverse();return shell(`<div class="action-row" style="margin-bottom:15px"><button class="btn" onclick="s.page='home';render()">+ Pesan lagi</button></div>${os.length?os.map(orderCard).join(''):`<div class="empty">Belum ada pesanan.</div>`}`,'Pesanan','Riwayat, pembayaran, antrean, dan order tracker dalam satu tempat.')}
function orderCard(o){let idx=steps.indexOf(o.status);if(idx<0&&o.status==='Dibatalkan')idx=-1;return `<div class="panel order-card"><div class="order-head"><div><div class="queue">#${o.queue_number}</div><strong>${esc(o.order_id)}</strong><div class="sub">${esc(o.created_at)}</div></div><div><span class="status ${o.status==='Dibatalkan'||o.status==='Pembayaran Ditolak'?'red':idx>=3?'':'blue'}">${esc(o.status)}</span><div style="text-align:right;margin-top:6px" class="sub">${esc(o.payment_status)}</div></div></div>${idx>=0?`<div class="timeline">${steps.map((x,i)=>`<div class="step ${i<idx?'done':i===idx?'current':''}">${esc(x.replace('Menunggu ','Menunggu '))}</div>`).join('')}</div>`:''}<div class="order-items">${o.items.map(i=>`<div class="item-line"><div style="font-size:24px">${EMOJI[(parseInt(i.product_id.slice(1))-1)%EMOJI.length]}</div><div class="grow"><strong>${esc(i.name)} × ${i.quantity}</strong><div class="sub">${i.king_size?'King Size · ':''}${i.addons?.map(a=>esc(a.name)).join(', ')||'Tanpa add-on'}</div>${i.notes?`<div class="notes">📝 ${esc(i.notes)}</div>`:''}</div><strong>${rupiah(i.quantity*i.unit_price)}</strong></div>`).join('')}</div><div class="total-row"><span>Total</span><span>${rupiah(o.total)}</span></div><div class="action-row" style="margin-top:14px">${['Menunggu Pembayaran','Menunggu Konfirmasi Penjual'].includes(o.status)?`<button class="btn coral" onclick="paymentPage('${o.order_id}')">${o.status==='Menunggu Pembayaran'?'Bayar sekarang':'Lihat pembayaran'}</button>`:''}<button class="btn" onclick="orderDetail('${o.order_id}')">Detail & Tracker</button>${o.status==='Menunggu Pembayaran'?`<button class="btn danger" onclick="cancelOrder('${o.order_id}')">Batalkan</button>`:''}${o.status==='Siap Diambil'&&o.pickup_reminder?.startsWith('Belum')?`<button class="btn" onclick="reminder('${o.order_id}')">Set pengingat</button>`:''}</div></div>`}
function orderDetail(id){let o=db.orders.find(x=>x.order_id===id);document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><div class="eyebrow">ORDER TRACKER</div><h2 class="serif" style="font-size:31px;margin:5px 0">Antrean #${o.queue_number}</h2><div class="sub">${esc(o.order_id)} · ${esc(o.created_at)}</div></div><button class="close" onclick="this.closest('.modal-backdrop').remove()">×</button></div>${orderCard(o)}<div class="panel" style="margin-top:12px"><strong>Pickup reminder</strong><div class="sub" style="margin-top:4px">${esc(o.pickup_reminder||'Belum ada pengingat')}</div></div></div></div>`)}
function cancelOrder(id){let o=db.orders.find(x=>x.order_id===id);if(o&&confirm('Batalkan pesanan ini?')){o.status='Dibatalkan';o.payment_status='CANCELLED';persist();render();toast('Pesanan dibatalkan')}}
function reminder(id){let o=db.orders.find(x=>x.order_id===id);if(o){o.pickup_reminder='Pengingat aktif — cek kembali saat pesanan sudah siap diambil.';persist();render();toast('Pengingat pesanan diaktifkan')}}
function sellerOverview(){let os=sellerOrders(),pending=os.filter(o=>o.status==='Menunggu Konfirmasi Penjual'),paid=os.reduce((a,o)=>a+((o.tenant_payments||[]).find(p=>p.tenant===user().tenant&&p.status==='PAID')?.amount||0),0),ps=db.products.filter(p=>p.tenant===user().tenant);return shell(`<div class="seller-stats"><div class="stat"><small>Pesanan Masuk</small><strong>${pending.length}</strong></div><div class="stat"><small>Pesanan Selesai / Masuk</small><strong>${os.length}</strong></div><div class="stat"><small>Pemasukan</small><strong>${rupiah(paid)}</strong></div><div class="stat"><small>Menu Aktif</small><strong>${ps.length}</strong></div></div><div class="panel" style="margin-top:16px"><div class="section-title" style="margin-top:0"><div><div class="tiny">QR PEMBAYARAN TENANT</div><h2>Payment setup</h2></div><span class="status ${qrUploads[user().tenant]?'':'warn'}">${qrUploads[user().tenant]?'QR READY':'Belum ada QR'}</span></div><p class="sub">Upload atau ganti QR pembayaran tenant. Format yang didukung: PNG.</p><label class="btn primary">Upload / Ganti QR<input type="file" accept=".png,image/png" hidden onchange="uploadQR(this)"></label>${qrUploads[user().tenant]?`<div class="qr" style="width:220px;margin-top:12px"><img src="${qrUploads[user().tenant]}"></div>`:''}</div><div class="panel" style="margin-top:16px"><div class="section-title" style="margin-top:0"><div><div class="tiny">PENDING</div><h2>Pesanan terbaru</h2></div><button class="btn" onclick="s.page='orders';render()">Kelola →</button></div>${pending.slice().reverse().slice(0,5).map(activityRow).join('')||'<div class="empty">Tidak ada pesanan menunggu konfirmasi.</div>'}</div>`,'Dashboard',`Selamat datang, ${esc(user().name)} · ${esc(user().tenant)}`)}
function activityRow(o){return `<div class="activity"><div class="queue">#${o.queue_number}</div><div class="grow"><strong>${esc(o.buyer_name)}</strong><div class="sub">${o.items.filter(i=>i.tenant===user().tenant).map(i=>esc(i.name)+' × '+i.quantity).join(', ')}</div></div><span class="status warn">${esc(o.status)}</span><button class="btn" onclick="sellerOrderDetail('${o.order_id}')">Detail</button></div>`}
function uploadQR(input){let f=input.files[0];if(!f)return;if(f.type!=='image/png'&&!f.name.toLowerCase().endsWith('.png'))return toast('HANYA MENDUKUNG FORMAT PNG');let r=new FileReader();r.onload=()=>{qrUploads[user().tenant]=r.result;db.users.find(u=>u.user_id===user().user_id).qr_path='browser://'+f.name;persist();render();toast('QR pembayaran tenant berhasil diunggah')};r.readAsDataURL(f)}
function sellerProducts(){let ps=db.products.filter(p=>p.tenant===user().tenant);return shell(`<div class="section-title"><div><div class="tiny">INVENTORY</div><h2>Manajemen Produk</h2><div class="sub">Tambah produk, update stok, atau hapus menu.</div></div><button class="btn primary" onclick="productForm()">＋ Tambah Produk</button></div><div class="panel"><div class="table-wrap"><table class="table"><thead><tr><th>Produk</th><th>Kategori</th><th>Harga</th><th>King</th><th>Stok</th><th>Status</th><th>Aksi</th></tr></thead><tbody>${ps.map(p=>`<tr><td><strong>${esc(p.name)}</strong><br><span class="sub">${p.id}</span></td><td>${esc(p.category)}</td><td>${rupiah(p.price)}</td><td>${rupiah(p.king_price||0)}</td><td><input class="input" style="width:90px" type="number" min="0" value="${p.stock}" onchange="setStock('${p.id}',this.value)"></td><td><span class="status ${p.stock?'':'red'}">${p.stock?'Available':'Habis'}</span></td><td><button class="btn danger" onclick="deleteProduct('${p.id}')">Hapus</button></td></tr>`).join('')}</tbody></table></div></div>`,'Produk','Kelola menu milik tenant kamu.')}
function setStock(id,v){let p=prod(id);if(p){p.stock=Math.max(0,parseInt(v)||0);persist();toast('Stok diperbarui')}}
function deleteProduct(id){let p=prod(id);if(p&&confirm(`Hapus ${p.name} dari database?`)){db.products=db.products.filter(x=>x.id!==id);persist();render();toast('Produk berhasil dihapus')}}
function productForm(){document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><div class="eyebrow">NEW MENU</div><h2 class="serif">Tambah Produk Baru</h2><div class="sub">Tenant: ${esc(user().tenant)}</div></div><button class="close" onclick="this.closest('.modal-backdrop').remove()">×</button></div><div class="field"><label>Nama Produk *</label><input id="pn" class="input"></div><div class="detail-grid"><div class="field"><label>Kategori *</label><select id="pc" class="select"><option>Rice Bowl</option><option>Snack</option><option>Minuman</option><option>Makanan Berat</option><option>Lainnya</option></select></div><div class="field"><label>Stok *</label><input id="ps" class="input" type="number" min="0" value="10"></div></div><div class="detail-grid"><div class="field"><label>Harga Produk *</label><input id="pp" class="input" type="number" min="0"></div><div class="field"><label>Harga King Size</label><input id="pk" class="input" type="number" min="0" value="0"></div></div><div class="field"><label>Deskripsi</label><textarea id="pd" class="textarea" rows="3"></textarea></div><button class="btn primary block" onclick="addProduct()">Simpan Produk</button></div></div>`)}
function addProduct(){let name=document.getElementById('pn').value.trim(),price=parseInt(document.getElementById('pp').value),stock=parseInt(document.getElementById('ps').value),king=parseInt(document.getElementById('pk').value)||0;if(!name||!Number.isFinite(price)||!Number.isFinite(stock))return toast('Lengkapi field wajib');let n=Math.max(0,...db.products.map(p=>parseInt(p.id.slice(1))||0))+1;db.products.push({id:'P'+String(n).padStart(3,'0'),tenant:user().tenant,name,category:document.getElementById('pc').value,price,stock,addons:structuredClone(db.addons),king_price:king,description:document.getElementById('pd').value.trim()});persist();document.querySelector('.modal-backdrop')?.remove();render();toast('Produk baru ditambahkan')}
function addons(){return shell(`<div class="section-title"><div><div class="tiny">CUSTOMIZATION</div><h2>Add-ons / Saus</h2><div class="sub">Daftar add-on global yang dipakai pada menu.</div></div><button class="btn primary" onclick="addonForm()">＋ Tambah Add-on</button></div><div class="food-grid">${db.addons.map((a,i)=>`<div class="panel"><div class="tiny">ADD-ON ${i+1}</div><h3 style="margin:6px 0">${esc(a.name)}</h3><strong>${rupiah(a.price)}</strong><div style="margin-top:13px"><button class="btn danger" onclick="deleteAddon(${i})">Hapus</button></div></div>`).join('')}</div>`,'Add-ons','Kelola pilihan tambahan yang tersedia untuk produk.')}
function addonForm(){document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><h2 class="serif">Tambah Add-on</h2><button class="close" onclick="this.closest('.modal-backdrop').remove()">×</button></div><div class="field"><label>Nama Add-on</label><input id="an" class="input" placeholder="Contoh: Saus BBQ"></div><div class="field"><label>Harga</label><input id="ap" class="input" type="number" min="0" value="0"></div><button class="btn primary block" onclick="addAddon()">Simpan</button></div></div>`)}
function addAddon(){let n=document.getElementById('an').value.trim(),p=parseInt(document.getElementById('ap').value)||0;if(!n)return toast('Nama add-on wajib diisi');db.addons.push({name:n,price:p});db.products.forEach(x=>x.addons=structuredClone(db.addons));persist();document.querySelector('.modal-backdrop')?.remove();render();toast('Add-on ditambahkan')}
function deleteAddon(i){if(confirm('Hapus add-on ini?')){db.addons.splice(i,1);db.products.forEach(x=>x.addons=structuredClone(db.addons));persist();render();toast('Add-on dihapus')}}
function sellerOrdersPage(){let os=sellerOrders().slice().reverse();return shell(`<div class="panel"><div class="table-wrap"><table class="table"><thead><tr><th>Antrean</th><th>Pembeli</th><th>Item</th><th>Pembayaran</th><th>Status</th><th>Aksi</th></tr></thead><tbody>${os.map(o=>`<tr><td><strong>#${o.queue_number}</strong><br><span class="sub">${esc(o.order_id)}</span></td><td>${esc(o.buyer_name)}</td><td>${o.items.filter(i=>i.tenant===user().tenant).map(i=>`${esc(i.name)} × ${i.quantity}${i.notes?`<br><span class="sub">📝 ${esc(i.notes)}</span>`:''}`).join('<br>')}</td><td><span class="status ${o.tenant_payments?.find(p=>p.tenant===user().tenant)?.status==='SUBMITTED'?'warn':''}">${esc(o.tenant_payments?.find(p=>p.tenant===user().tenant)?.status||o.payment_status)}</span></td><td><span class="status">${esc(o.status)}</span></td><td><button class="btn primary" onclick="sellerOrderDetail('${o.order_id}')">Detail</button></td></tr>`).join('')}</tbody></table></div></div>`,'Pesanan','Konfirmasi pembayaran, perhatikan catatan, dan proses order.')}
function tenantPayment(o){return (o.tenant_payments||[]).find(p=>p.tenant===user().tenant)||{tenant:user().tenant,amount:o.items.filter(i=>i.tenant===user().tenant).reduce((a,i)=>a+i.quantity*i.unit_price,0),status:o.payment_status,proof_path:''}}
function sellerOrderDetail(id){let o=db.orders.find(x=>x.order_id===id),p=tenantPayment(o),items=o.items.filter(i=>i.tenant===user().tenant);document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><div class="eyebrow">ORDER DETAIL</div><h2 class="serif">Antrean #${o.queue_number}</h2><div class="sub">${esc(o.order_id)} · ${esc(o.buyer_name)}</div></div><button class="close" onclick="this.closest('.modal-backdrop').remove()">×</button></div><div class="panel" style="margin-top:15px"><div><strong>Status:</strong> ${esc(o.status)}</div><div style="margin-top:6px"><strong>Pembayaran tenant:</strong> ${esc(p.status)} · ${rupiah(p.amount)}</div>${paymentProofs[o.order_id+'|'+p.tenant]?`<img src="${paymentProofs[o.order_id+'|'+p.tenant]}" style="width:180px;height:180px;object-fit:contain;border:1px solid var(--line);border-radius:12px;margin-top:10px">`:''}</div><div class="panel" style="margin-top:12px"><strong>Item & Catatan Khusus</strong>${items.map(i=>`<div class="item-line"><div class="grow"><strong>${esc(i.name)} × ${i.quantity}${i.king_size?' · King Size':''}</strong><div class="sub">${i.addons?.map(a=>esc(a.name)).join(', ')||'Tanpa add-on'}</div>${i.notes?`<div class="notes">📝 CATATAN KHUSUS PEMBELI<br><b>${esc(i.notes)}</b></div>`:`<div class="sub">📝 Tidak ada catatan</div>`}</div></div>`).join('')}</div><div class="action-row" style="margin-top:14px">${o.status==='Menunggu Konfirmasi Penjual'?`<button class="btn primary" onclick="confirmSeller('${o.order_id}')">✓ Konfirmasi Pesanan</button><button class="btn danger" onclick="rejectSeller('${o.order_id}')">✕ Tolak Bukti</button>`:''}${o.status==='Sedang Diproses'?`<button class="btn" onclick="readySeller('${o.order_id}')">✓ Tandai Siap Diambil</button>`:''}</div></div></div>`)}
function confirmSeller(id){let o=db.orders.find(x=>x.order_id===id),p=tenantPayment(o);if(p.status!=='SUBMITTED')return toast('Seller hanya dapat mengonfirmasi setelah buyer mengunggah bukti');let items=o.items.filter(i=>i.tenant===user().tenant);for(const i of items){let x=prod(i.product_id);if(!x||x.stock<i.quantity)return toast(`Stok ${i.name} tidak mencukupi`)}items.forEach(i=>{let x=prod(i.product_id);x.stock-=i.quantity});p.status='PAID';o.payment_status=(o.tenant_payments||[]).every(x=>x.status==='PAID')?'PAID':'PARTIAL';o.status=o.payment_status==='PAID'?'Sedang Diproses':'Menunggu Konfirmasi Penjual';persist();document.querySelector('.modal-backdrop')?.remove();render();toast('Pesanan dikonfirmasi dan stok dikurangi')}
function rejectSeller(id){let o=db.orders.find(x=>x.order_id===id),p=tenantPayment(o);if(!p.proof_path&&!paymentProofs[id+'|'+p.tenant])return toast('Belum ada bukti pembayaran');p.status='REJECTED';o.status='Pembayaran Ditolak';o.payment_status='REJECTED';persist();document.querySelector('.modal-backdrop')?.remove();render();toast('Bukti pembayaran ditolak')}
function readySeller(id){let o=db.orders.find(x=>x.order_id===id);o.status='Siap Diambil';persist();document.querySelector('.modal-backdrop')?.remove();render();toast('Pesanan ditandai siap diambil')}
function history(){let a=db.archived_orders||[];return shell(`<div class="panel"><div class="section-title" style="margin-top:0"><div><div class="tiny">ARCHIVE</div><h2>Riwayat Pesanan</h2><div class="sub">Data yang sudah diarsipkan oleh sistem backend.</div></div><button class="btn danger" onclick="clearHistory()">Bersihkan Riwayat</button></div>${a.length?a.map(o=>`<div class="order-row"><div class="queue">#${o.queue_number||'-'}</div><div class="grow"><strong>${esc(o.order_id)}</strong><div class="sub">${esc(o.buyer_name||'')} · ${esc(o.created_at||'')}</div></div><span class="status">${esc(o.status||'Archived')}</span></div>`).join(''):`<div class="empty">Belum ada order yang diarsipkan.</div>`}</div>`,'Riwayat','Arsip pesanan sesuai struktur data backend.')}
function clearHistory(){if(confirm('Bersihkan seluruh riwayat arsip?')){db.archived_orders=[];db.archived_sales={};persist();render();toast('Riwayat dibersihkan')}}
function reports(){let os=sellerOrders(),paid=os.filter(o=>o.tenant_payments?.find(p=>p.tenant===user().tenant)?.status==='PAID'),rev=paid.reduce((a,o)=>a+tenantPayment(o).amount,0),counts={};paid.forEach(o=>o.items.filter(i=>i.tenant===user().tenant).forEach(i=>counts[i.name]=(counts[i.name]||0)+i.quantity));let rows=Object.entries(counts).sort((a,b)=>b[1]-a[1]);return shell(`<div class="seller-stats"><div class="stat"><small>Pemasukan</small><strong>${rupiah(rev)}</strong></div><div class="stat"><small>Paid orders</small><strong>${paid.length}</strong></div><div class="stat"><small>Unit terjual</small><strong>${rows.reduce((a,[,n])=>a+n,0)}</strong></div><div class="stat"><small>Produk terlaris</small><strong style="font-size:18px">${rows[0]?esc(rows[0][0]):'—'}</strong></div></div><div class="panel" style="margin-top:16px"><div class="section-title" style="margin-top:0"><div><div class="tiny">ANALYTICS</div><h2>Sales report</h2></div><select class="select" style="width:150px" id="period"><option>Harian</option><option>Mingguan</option><option>Bulanan</option><option>Akumulasi</option></select></div><div class="food-grid">${rows.map(([n,c])=>`<div class="stat"><small>${esc(n)}</small><strong>${c}</strong><div class="sub">unit terjual</div></div>`).join('')||'<div class="empty" style="grid-column:1/-1">Belum ada penjualan berbayar.</div>'}</div><p class="sub" style="margin-top:16px">Mode laporan mengikuti pilihan backend: Harian, Mingguan, Bulanan, atau Akumulasi. Dataset browser menyimpan agregasi lokal untuk prototype.</p></div>`,'Laporan','Ringkasan penjualan tenant berdasarkan order yang sudah dibayar.')}
function render(){if(!s.role)return entrance();if(s.role==='buyer'){if(s.page==='home')document.body.innerHTML=buyerHome();else if(s.page==='tenant')document.body.innerHTML=tenants();else if(s.page==='cart')document.body.innerHTML=cartPage();else if(s.page==='wishlist')document.body.innerHTML=wishlistPage();else document.body.innerHTML=ordersPage()}else{if(s.page==='overview')document.body.innerHTML=sellerOverview();else if(s.page==='products')document.body.innerHTML=sellerProducts();else if(s.page==='addons')document.body.innerHTML=addons();else if(s.page==='orders')document.body.innerHTML=sellerOrdersPage();else if(s.page==='history')document.body.innerHTML=history();else document.body.innerHTML=reports()}}
render();
